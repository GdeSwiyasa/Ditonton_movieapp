import 'package:flutter/material.dart';

class DetailTvPage extends StatelessWidget {
  static const ROUTE_NAME = '/detail-tv';
  const DetailTvPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
